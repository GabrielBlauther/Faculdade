/*6) Um dos métodos utilizados para calcular o MDC é o processo das divisões sucessivas, ou método de
Euclides. Através deste método, efetuamos várias divisões até chegar a uma divisão exata (resto 0). O
divisor da divisão exata é o MDC A regra prática para o cálculo do MDC é:
1) Dividimos o maior número (dividendo) pelo menor número (divisor);
2) O divisor da conta anterior (passo 1) torna-se o dividendo e o resto torna-se o divisor. Uma nova
divisão é efetuada com os novos dividendo e divisor.
3) O divisor da conta anterior (passo 2) torna-se o dividendo e o resto torna-se o divisor. Uma nova
divisão é efetuada com os novos dividendo e divisor.
4) As divisões continuam até que o resto da divisão seja 0.*/

#include <stdio.h>

int main(){
  int
}